{
  "buildCommand": "npm run install:all && npm run build:api && npm run build:frontend",
  "outputDirectory": "frontend/dist",
  "public": "frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
