{
  "buildCommand": "npm run install:all && npm run build:frontend",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/src/vercel/index.ts": {
      "runtime": "@vercel/node@3.0.0",
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "api/**"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/src/vercel/index.ts" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
